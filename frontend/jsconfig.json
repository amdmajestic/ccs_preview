{
    "compilerOptions": {
      "baseUrl": ".",  // Set base URL to root of the project
    "paths": {
    "/content/*": ["public/*"],  // Map /content/* to public/*
    "/src/*": ["src/*"]  // Map /src/* to src/*
    }
},
"exclude": ["node_modules"]
}
